# Customizations

本文档用于记录本仓库相对上游项目的本地差异、个性化要求和后续维护约定。升级或同步上游代码前，优先查看本文件，避免覆盖本地定制。

## 维护原则

- `README.md` 尽量保持接近上游，只记录通用项目说明。
- 本地差异、部署偏好、专属配置说明、二次开发要求统一记录在 `CUSTOMIZATIONS.md`。
- 敏感信息不写入仓库，包括邮箱凭据、API Token、Clash Secret、数据库密码和通知 Webhook。
- 每次引入和上游不同的行为、配置、依赖、脚本或界面改动，都需要在“差异记录”中追加一条。
- 同步上游前先备份 `data/` 目录，并对照本文件确认哪些本地改动需要保留。

## 本地环境要求

- 本地项目目录：`f:\F\pycharm\openai-cpa`
- 本地时区：`Asia/Shanghai`
- Web Console 默认访问：`http://127.0.0.1:8000`
- Windows 原生运行优先使用 Python 3.12.x。
- Linux / macOS 原生运行按上游说明使用 Python 3.11。
- Docker 部署时保留 `./data:/app/data` 持久化挂载，避免升级或重建容器时丢失运行数据。

## 个性化要求

- 优先通过 Web Console 管理配置、启动停止任务、查看日志和维护库存。
- 本地运行配置以 `data/` 目录内实际配置为准，`config.example.yaml` 仅作为默认值和升级补全参考。
- 本地部署和测试时优先保持可回滚：修改配置、代理池、邮箱后端、仓库 API 或通知渠道前先记录变更原因。
- 与上游不同的前端交互、后端接口、数据库结构或 Docker 编排，都必须在本文件写明影响范围和验证方式。

## 差异记录

| 日期 | 类型 | 差异内容 | 影响范围 | 验证 |
| --- | --- | --- | --- | --- |
| 2026-05-09 | 文档 | 新增 `CUSTOMIZATIONS.md`，专门记录本地与上游不同的个性化要求。 | 文档维护 | 已创建文件 |
